import { Tabs } from 'expo-router';
import { Ionicons } from '@expo/vector-icons'; // İconlar için

export default function TabLayout() {
  return (
    <Tabs
      screenOptions={{
        headerShown: false,
      }}
    >
    </Tabs>
  );
}
